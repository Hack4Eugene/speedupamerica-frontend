// import {expect} from 'chai';
// import * as sinon from 'sinon';
// import * as HttpStatus from 'http-status-codes';

// import {logging, base} from '../../src/common/logging';

describe('Logging', () => {
  it('should have metadata on base log', () => {

  });

  it('should have type=app on child log', () => {

  });

  it('should not add loggly if LOGGLY_TOKEN not set', () => {

  });

  it('should add Loggy if LOGGLY_TOKEN set', () => {

  });
});
