version: '3.2'
services:
  frontend-v2:
    build: .
    env_file:
      - local.env
    ports:
      - "8081:8081"
    volumes:
      - .:/app
      - /app/node_modules
networks:
  default:
    external:
      name: speedupamerica_default
